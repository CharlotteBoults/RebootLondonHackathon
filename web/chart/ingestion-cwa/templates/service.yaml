apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingestion-cwa.fullname" . }}
  labels:
    {{- include "ingestion-cwa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingestion-cwa.selectorLabels" . | nindent 4 }}
